'use strict';

/**
 * what-they-say-comment service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::what-they-say-comment.what-they-say-comment');
